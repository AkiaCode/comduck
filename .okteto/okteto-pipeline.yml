build:
  comduck:
    args:
      - NODE_ENV=development
deploy:
  - helm upgrade --install comduck chart --set image=${OKTETO_BUILD_comduck_IMAGE} --set url="https://comduck-$(kubectl config view --minify -o jsonpath='{..namespace}').cloud.okteto.net" --set environment=development
